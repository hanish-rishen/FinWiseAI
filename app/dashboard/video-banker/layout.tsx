import "./global.css";

export const metadata = {
  title: "AI Branch Manager | FinWiseAI",
  description: "Video conversation with our AI Branch Manager",
};

export default function VideoLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
